apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: task-manager-app
  namespace: flux-system

spec:
  interval: 1m

  path: ./overlays/dev

  prune: true

  sourceRef:
    kind: GitRepository
    name: kustomize-flux-gitops

  targetNamespace: kustomize-flux-demo